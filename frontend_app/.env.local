# Backend base URL for proxy rewrites (must match the running FastAPI service)
# The Next.js rewrites map /api/proxy/:path* -> ${NEXT_PUBLIC_BACKEND_URL}/:path*
NEXT_PUBLIC_BACKEND_URL=https://vscode-internal-32364-beta.beta01.cloud.kavia.ai:3001
